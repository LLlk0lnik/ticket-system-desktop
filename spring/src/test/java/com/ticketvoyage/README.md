# как запускать эти тесты? 🤔

ну короче, тут у нас разные тесты для нашего ticket-system-desktop приложения.
я постарался все структурировать, чтобы было удобно тестировать разные части.

## какие тесты есть:

- **DataServiceTest** - проверяет работу с данными (юнит-тесты)
- **FunctionalTest** - тестирует регистрацию и логин пользователей
- **IntegrationTest** - интеграционные тесты (как все работает вместе)
- **PerformanceTest** - тесты производительности
- **TestRunner** - запускает все тесты подряд

## как запускать:

### вариант 1: все тесты сразу (самый простой)
**Через терминал:**
```bash
./run_tests.sh
```
этот скрипт сам все скомпилирует и запустит по порядку

**Через IDEA на Windows:**
1. Открой проект в IntelliJ IDEA
2. Правой кнопкой на `run_tests.sh` → Run (или Edit Configurations)
3. В настройках: Working directory = `$PROJECT_DIR$`
4. Запустить

### вариант 2: запустить конкретный тест

**Через IDEA (рекомендуется для Windows):**
1. Открой проект в IDEA
2. Убедись что JDK настроена (File → Project Structure → Project SDK)
3. Добавь JAR-файлы в classpath:
   - File → Project Structure → Modules → Dependencies
   - Нажми `+` → JARs or directories
   - Выбери папку `lib/` в корне проекта
4. Для каждого теста:
   - Правой кнопкой на файле теста → Run (или Create 'TestClass.main()')
   - IDEA автоматически создаст конфигурацию запуска

**Через командную строку Windows:**
```cmd
# сначала скомпилировать
javac -cp "lib/*" -d bin src/main/java/com/ticketvoyage/*.java src/main/java/com/ticketvoyage/*/*.java src/test/java/com/ticketvoyage/*.java

# потом запустить нужный тест
java -cp "bin:lib/*" com.ticketvoyage.DataServiceTest
java -cp "bin:lib/*" com.ticketvoyage.FunctionalTest
java -cp "bin:lib/*" com.ticketvoyage.IntegrationTest
java -cp "bin:lib/*" com.ticketvoyage.PerformanceTest
```

### вариант 3: через TestRunner
**Через IDEA:**
- Правой кнопкой на `TestRunner.java` → Run 'TestRunner.main()'

**Через командную строку:**
```cmd
java -cp "bin:lib/*" com.ticketvoyage.TestRunner
```
он сам все тесты по порядку запустит

### дополнительные тесты в корне проекта:
Также есть отдельные тестовые файлы в корне (не в src/test/):
- `compile_test.java` - проверка компиляции
- `test_auto_login.java` - тест автоматического входа
- `test_db.java` - тест подключения к БД
- `test_profile.java` - тест профиля пользователя
- `test_clean.java` - тест очистки данных
- `test_warnings.java` - тест предупреждений

**Запуск в IDEA:**
- Правой кнопкой на любом .java файле в корне → Run

## что тестируется:

### DataServiceTest:
- поиск пользователя по email
- получение всех маршрутов
- работа с базой данных

### FunctionalTest:
- регистрация нового пользователя
- логин пользователя
- проверка прав доступа

### IntegrationTest:
- как все компоненты работают вместе
- бизнес-логика

### PerformanceTest:
- скорость работы
- нагрузочные тесты

## база данных для тестов

тесты используют базу данных `ticketvoyage.db` в корне проекта.
она должна быть настроена правильно, иначе тесты могут падать.

если что-то не работает - проверь:
1. есть ли база данных
2. правильно ли настроены пути
3. все ли зависимости в папке lib/

## если тесты падают:

- проверь консоль на ошибки
- убедись что база данных существует
- попробуй пересобрать проект
- проверь что все jar-файлы в lib/

в общем, тесты должны помогать проверять что код работает правильно.
если что-то сломается - тесты покажут где именно. 🚀
