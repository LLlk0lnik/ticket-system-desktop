# как запускать проект в IntelliJ IDEA на Windows 💻

эй, если ты на винде и используешь IDEA - вот тебе подробная инструкция как все настроить и запустить!

## 1. настройка проекта

### открываем проект:
- File → Open
- Выбираем папку `spring` (где лежит этот README)
- IDEA должна автоматически распознать Java проект

### проверяем JDK:
- File → Project Structure (Ctrl+Alt+Shift+S)
- Project → Project SDK
- Если JDK нет - нажми New → Download JDK
- Выбери версию 11 или выше

### добавляем библиотеки:
- В Project Structure → Modules
- Выбираем модуль проекта
- Dependencies вкладка
- Нажимаем `+` → JARs or directories
- Выбираем папку `lib/` в корне проекта
- Применяем изменения (Apply → OK)

## 2. запуск основного приложения

### вариант 1: через Main.java
- Найди файл `src/main/java/com/ticketvoyage/Main.java`
- Правой кнопкой → Run 'Main.main()'
- Или Edit Configurations → `+` → Application
  - Main class: `com.ticketvoyage.Main`
  - Working directory: `$PROJECT_DIR$`
  - Use classpath of module: выбери основной модуль

### вариант 2: через TicketVoyageApp.java
- Если хочешь запустить GUI версию
- Правой кнопкой на `TicketVoyageApp.java` → Run

## 3. запуск тестов

### все тесты сразу:
- Правой кнопкой на `src/test/java/com/ticketvoyage/TestRunner.java`
- Run 'TestRunner.main()'

### отдельные тесты:
- `DataServiceTest.java` - тесты базы данных
- `FunctionalTest.java` - функциональные тесты
- `IntegrationTest.java` - интеграционные тесты
- `PerformanceTest.java` - тесты производительности

Просто правой кнопкой на файле → Run

### дополнительные тесты в корне:
В корне проекта есть еще тестовые файлы:
- `compile_test.java` - проверка компиляции
- `test_auto_login.java` - тест автоматического входа
- `test_db.java` - тест БД
- `test_profile.java` - тест профиля

Запускаются так же - правой кнопкой → Run

## 4. если что-то не работает

### ошибка "class not found":
- Проверь что все JAR из папки `lib/` добавлены в classpath
- File → Project Structure → Modules → Dependencies

### база данных не находится:
- Проверь что файл `ticketvoyage.db` есть в корне проекта
- Если нет - запусти приложение сначала, оно создаст базу

### кодировка в консоли:
- Если русские символы отображаются криво:
- Help → Edit Custom VM Options
- Добавь строку: `-Dfile.encoding=UTF-8`

## 5. полезные горячие клавиши

- `Shift+F10` - запустить последнее
- `Shift+F9` - отладка
- `Ctrl+F9` - скомпилировать проект
- `Ctrl+Shift+F10` - запустить текущий файл

## 6. структура проекта

```
spring/
├── src/main/java/com/ticketvoyage/  # основной код
│   ├── Main.java                    # консольная версия
│   ├── TicketVoyageApp.java         # GUI версия
│   ├── model/                       # модели данных
│   ├── service/                     # бизнес-логика
│   └── ui/                          # интерфейс
├── src/test/java/com/ticketvoyage/  # тесты
├── lib/                             # библиотеки (JAR)
├── bin/                             # скомпилированные классы
└── ticketvoyage.db                  # база данных
```

удачи с разработкой! если что-то неясно - пиши, помогу разобраться 🚀


